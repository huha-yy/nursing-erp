# nursing-erp 业务完整度评估与路线图

> 评估日期：2026-08-21 · 评估范围：nursing-erp（含与 ai-nursing-home 的联动）
> 状态：**讨论稿** —— 第 5 节的决策点定稿后更新为正式路线图
> 姊妹文档：`ai-nursing-home/JOURNAL.md`（平台侧决策日志）

---

## 1. 现状总评

作为 MVP，核心闭环已经打通，且有两个真正的差异化亮点：

- **膳食模块全系统最强**：周菜单/点餐状态机/改退留痕/月结，叠加 OCR+LLM 流水线
  （拍照→识别→纠错→菜品库容错匹配→自动建单），真实解决了手工录入痛点
- **老人生命周期视图**（`residents/views resident_lifecycle`）：九类记录汇聚时间线
  + ECharts 健康趋势，站在"照护连续性"角度设计
- **与 AI 侧联动**：`/api/` 供 dato-control 的 Dashboard/Chat 读写，"业务底座 +
  智能入口"组合是市面 ERP 没有的形态

主要短板：**财务薄、医疗薄、床位无台账**——距离"真 ERP"最大的差距在收费业务。

## 2. 已完成的关键加固（2026-08-21）

| 项 | 状态 | 提交 |
|---|---|---|
| `/api/` 匿名裸奔（P0，公网可读老人 PII/伪造数据/盗刷 OCR 配额） | ✅ 已修复：API key + session 双认证，轻量页登录保护，39 测试全绿 | `6c8b46e` |
| 依赖锁文件缺失 | ✅ uv.lock 已提交（Django 5.2.17 实测符合 `<6` 约束） | `80c6665` |

> 勘误：早期评估称"venv 实际 Django 6.0.7 超出 pyproject 约束"——实测不成立，
> venv 为 Django 5.2.17（2026-08-21 复核）。教训：自动化勘察结果需实测验证。

## 3. 业务完整度矩阵

| 业务域 | 完整度 | 已有能力 | 缺口 | 建议优先级 |
|---|---|---|---|---|
| 膳食 | ★★★★★ | 菜单/点餐/状态机/改退留痕/月结/OCR 录入 | — | 维护 |
| 库存/后勤 | ★★★★☆ | 安全库存告警、出入库自动增减、补货 action、报修/巡检/审批 | 供应商管理、食品效期 | P3 |
| 老人档案/照护 | ★★★☆☆ | 档案+楼栋+等级、护理日志、健康/用药/作息记录、生命周期视图 | **床位台账**（room 是字符串，1752 床无占用/空床视图）、**入住评估流程**（能力评估→定级，民政监管要求）、护理计划（只有记录没有计划） | **P1** |
| 排班/考勤 | ★★★☆☆ | 12h 白/夜班排班、考勤打卡、任务派发、绩效 | 换班/调班申请流、资质证书管理（护理员持证是检查重点） | P3 |
| 财务 | ★★★☆☆ | **应收月账单**（床位+护理+餐费合并出账/全额核销/欠费名单/后台价目表）、餐费月结 | 押金、退费、医保对接、部分缴费流水 | P3 |
| 医疗协同 | ★★☆☆☆ | MedicationRecord 用药记录 | "医嘱→发药→核对"安全闭环、药品库存 | P3 |
| 家属服务/监管报送 | ☆☆☆☆☆ | 无 | 家属端查询、民政系统数据上报 | 待定（见 Q6） |

## 4. 工程隐患清单（按优先级）

| # | 隐患 | 影响 | 优先级 |
|---|---|---|---|
| 1 | ~~`/api/` 无认证~~ | — | ✅ 已修复（2026-08-21） |
| 2 | ~~**staff 与 residents 无外键**：`NursingLog.staff_name`、`Task.assigner_name`、`StockIn.operator` 均为纯字符串~~ | — | ✅ 已修复（2026-08-21，11 个 FK 字段；回填 74 条全挂接——11 条刘主任重名经定夺全挂 1号楼，0 遗留） |
| 3 | ~~**API 层无楼栋过滤**：admin 有 `BuildingScopeMixin`，但 `/api/` 全量返回~~ | — | ✅ 已修复（2026-08-21，X-Building 头 + session 档案双路径，越权 404/403） |
| 4 | **宿主机 runserver 撑公网流量**：无进程守护，进程挂了公网即 502 | 可用性风险 + runserver 本不适合生产 | P2 · 已定暂缓（Q5，08-24：开发期保留热部署，收尾后切换） |
| 5 | 照护分区/护理等级等硬编码 choices | 第二家客户（社会福利中心，4 照护分区）直接复用会不对 | P2（客户适配前，见 Q4） |

## 5. 决策点讨论记录

| # | 决策点 | 结论 | 状态 |
|---|---|---|---|
| Q1 | 床位管理粒度 | **四级模型**：楼/层/房间/**床位实体**——床位是独立实体，可统计入住率/空床。数据模型一次到位，UI 可先糙。1752 床机构的入住率是院长最常看的数字 | ✅ 定稿 08-21 |
| Q3 | 财务模块范围 | **先做应收月账单**：床位费+护理费+餐费合并出账、缴费核销、欠费名单。与现有 MealFinance 天然衔接，演示价值最高；押金/退费/医保等真实对接需求明确后再加 | ✅ 定稿 08-21 |
| Q6 | 家属端/监管报送 | **暂不做功能**，但模型/API 分层设计时预留"角色"概念（用户类型可扩展家属角色），不提前实现 | ✅ 定稿 08-21 |
| Q2 | staff FK 存量回填策略 | **脚本按姓名精确匹配 Employee，匹配不上（错字/同名/离职）置空并输出清单人工补**。当前仅 8 条日志/41 员工，量小；不改造则日志攒多后回填成本剧增 | ✅ 定稿 08-21 |
| Q4 | 第二家客户时间表 | 决定配置化改造（护理等级/照护分区/楼栋数 → 机构档案）优先级；建议无论时间表如何都提前抽象 | 🟡 待定 |
| Q5 | 部署形态切换 | **暂缓，开发期保留 runserver**（2026-08-24 定）：autoreload 热部署对当前高频改码价值最大，公网 502 风险由开发节奏换取；待功能开发真正收尾后执行切换（gunicorn 已在依赖、whitenoise/静态配置均已就绪，届时仍是半小时活） | ✅ 定稿 08-24（暂缓） |

## 6. 路线图（依据已定稿决策更新）

| 阶段 | 内容 | 状态 |
|---|---|---|
| 阶段一 · 数据地基 | **床位台账（四级模型）** ← Q1 定稿；**staff FK 改造 + API 楼栋过滤** ← Q2 定稿 | 床位台账 ✅ 08-21 已上线（36 位老人全部挂床、6 栋 36 床、入住率 100%）；staff FK + API 楼栋过滤 ✅ 08-21 已上线 |
| 阶段二 · 收费与评估 | **应收月账单**（Q3 定稿）+ 入住评估→定级流程 | 应收月账单 ✅ 08-21 已上线（36 张 ¥85,555 出账勾稽通过）；入住评估 ⚪ 未启动 |
| 阶段三 · 运营深化 | 换班流、资质证书、医嘱闭环、供应商/效期 | ⚪ 按需 |
| 阶段四 · 多机构适配 | 分区/等级/楼栋配置化（待 Q4） | ⚪ 待定 |
| 并行 · 部署加固 | gunicorn + systemd + 备份演练 | ⏸ 暂缓（Q5 定稿 08-24：开发期保留 runserver 热部署，开发完成后执行） |

> 床位台账落地记录（2026-08-21）：新建 `beds` app（楼栋/楼层/房间/床位四级），
> `Resident.bed` 挂接 + 一人一床条件唯一约束；字符串三列保留为兼容缓存，
> `save()` 自动以床位链为准同步。回填命令 `backfill_beds` 幂等跑通（36/36 链接）。
> 新增 `GET /api/beds/`、`GET /api/beds/occupancy/` 与看板页 `/beds/`；
> `/api/residents/` 契约零变动（回归钉测试锁定）。admin 楼栋隔离补上了
> Bed 下拉的跨楼栋写漏洞。部署序：备份 → migrate → dry-run → 回填 → 幂等复跑验证。

> staff FK + API 楼栋过滤落地记录（2026-08-21）：
> **FK 改造**：`StaffFkMixin`（`nursing_erp/staff_fk.py`）+ 11 个 FK 字段
> （护理日志/等级变更/任务/出入库/维修/巡检/审批/异常/点餐/改餐），
> 字符串列保留为兼容缓存，save() 三分支同步（FK 为准 / 唯一姓名自动挂 /
> 歧义保持空）。回填 `backfill_staff_fk`：63 自动挂接；11 条"刘主任"重名
> （Employee 表两人同名：1号楼 id=7 / 5号楼 id=11）经用户定夺**全挂 1号楼**
> （上下文证据：张国栋住 1号楼、任务执行人均 1号楼、无一条指向 5号楼），
> 定向 update 后幂等重跑清零——**74/74 全挂接，0 歧义 0 无匹配**。提交 `846d083`。
> **楼栋过滤**：`nursing_erp/api_scope.py`——key 路径读 `X-Building` 头
> （中文楼栋名 percent-encode 传输，`f4649e8`），未知名 400 fail-loud；
> session 路径取 `Employee.building` 且忽略该头（防伪造）。列表 AND 叠加、
> 详情统一 404、写路径 403、batch 预检整批拒绝、beds 统计覆盖语义；
> 顺带修复两处缺失 id 裸 get 500。提交 `ffa1c9a`。AI 侧配套（dl-control
> 头透传 + 堵两无鉴权路由 + 修 meal-query 404）见 ai-nursing-home
> `4c95e7a`/`6ff75b7`，联调全绿（楼长仅本楼 / 院长全院 / 匿名 401）。

> 应收月账单落地记录（2026-08-21）：新建 `billing` app——`FeeRule` 价目表
> （后台可改，缺行 fail-loud）+ `MonthlyBill`（一人一账期一张单，整月计费，
> Decimal 全链，已缴费冻结，核销/撤销幂等，核销人自动挂员工档案）。
> 餐费以 MealFinance 为唯一数据源、无点餐不造幽灵行（meals/ 一行未改）。
> 种子价目：床位 800/月，护理 自理300/半护1200/全护2000/失智2400/月，
> 餐费 15/餐（0002 数据迁移，admin 改价不回写）。API 6 端点 + /billing/
> 看板（出账/逐单核销/欠费名单）+ admin 核销 actions（逐条 save 防
> queryset.update 绕过 mixin）。测试 18 个，全套 102 绿。提交 `31425ef`。
> 生产 2026-08 已出账 36 张 ¥85,555：床位+护理 70,600 与计划锚点分毫不差；
> 餐费 14,955 vs 锚点 13,320 的差额全部来自张国栋名下 171 条演示重复点餐
> （月结行刷新 930→2,565，口径内行为；点餐去重为独立数据治理事项）。

> 设计原则（Q6 结论）：新增模型/API 时预留角色扩展位，不为未来功能提前写代码。
